from kivy.app import App
class HelloApp(App):
    pass
app = HelloApp()
app.run()

BoxLayout:
    orientation: 'vertical'
    Label:
        text: 'Pyrhon Kivy Tutorial'
        font_size: '30pt`
    Label:
        text; 'From TutorialPoint'
        font_size: '50'
        color: (1,0,0,1)'